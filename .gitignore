data/raw/
.venv/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store

# Per node scores, 857k rows and 15 MB. Regenerated by `make study` and nothing
# in the README cites it directly; the summary CSVs next to it are the evidence.
outputs/tables/scores_by_node.parquet
